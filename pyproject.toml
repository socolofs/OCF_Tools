# This file is used by pip to install the ocf_tools package
# 
# To install an editable version, use:
# 
#    pip install --editable .
# 
# For a standard, non-editable installation, use:
# 
#    pip install .
# 
# 
# Author
# ------
# S. Socolofsky, June 2026, Texas A&M University, <socolofs@tamu.edu>

# Define the build system requirements
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

# Project metadata
[project]
name = "ocf_tools"
version = "1.0.0"
license = { file = "LICENSE.txt" }
description = "Computational Tools for Open Channel Hydraulics"
authors = [
    {name = "Scott A. Socolofsky", email = "socolofs@tamu.edu"}
]
maintainers = [
    { name = "Scott A. Socolofsky", email="socolofs@tamu.edu" },
]
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.25.2",
    "scipy>=1.13.0",
    "matplotlib>=3.6.2",
    "pandas>=1.5.2",
    "pytest>=7.1.2",
    "openpyxl>=3.0.10"  
]
readme = "README.rst"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Engineering",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Programming Language :: Python 3",
    "Programming Language :: Python 3.11",
    "Topic :: Software Development :: Libraries",
    "Topic :: Scientific/Engineering",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: MacOS",
]

# Package discovery
[tool.setuptools.packages.find]
where = ["."]
include = ["ocf_tools*"]

# If running the tests or building the documentation
[project.optional-dependencies]
test = [
    "pytest",
]
doc = [   # Copied from Scipy
    "sphinx>=5.0.0,<8.2.0",
    "intersphinx_registry",
    "pydata-sphinx-theme>=0.15.2",
    "sphinx-copybutton",
    "sphinx-design>=0.4.0",
    "matplotlib>=3.5",
    "numpydoc",
    "jupytext",
    "myst-nb>=1.2.0",
    "pooch",
    "jupyterlite-sphinx>=0.19.1",
    "jupyterlite-pyodide-kernel",
    "linkify-it-py"
]